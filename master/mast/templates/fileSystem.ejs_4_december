<div class="fileSystem-template mast-ui-table">
	<div class="organization-bar clearfix">
		<div class="dir-files dir-left {{column1.className}} mast-critical mast-column-header"><span>{{column1.name}}</span></div>
		<div class="dir-middle {{column2.className}} mast-non-critical mast-column-header"><span>{{column2.name}}</span></div>
		<div class="dir-right {{column3.className}} mast-non-critical mast-column-header"><span>{{column3.name}}</span></div>
	</div>
	<div id="fileSystem-outlet" class="clearfix"></div>
</div>